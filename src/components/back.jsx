import React from "react";
import "./navbar.css"; // Import file CSS

const Back = () => {
  return (
    <nav>
      <ul>
        <li><a href="/home">Back to Home</a></li>
      </ul>
    </nav>
  );
};

export default Back;